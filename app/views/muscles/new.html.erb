<h1>新規投稿</h1>
  <%= form_with model: @muscle do |f| %>
    <h4>イメージ</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>タイトル</h4>
      <%= f.text_field :title %>
    <h4>説明</h4>
      <%= f.text_field :caption %>
    <%= f.submit '投稿' %>
  <% end %>